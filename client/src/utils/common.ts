// This file is deprecated. Import utilities from '@/lib/utils' instead.
import { 
  formatDate,
  formatCurrency,
  truncateText,
  generatePageTitle,
  debounce,
  formatFileSize,
  getInitials,
  formatPhoneNumber
} from '@/lib/utils';

export {
  formatDate,
  formatCurrency,
  truncateText,
  generatePageTitle,
  debounce,
  formatFileSize,
  getInitials,
  formatPhoneNumber
};